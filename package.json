{
  "name": "budget-tracker",
  "version": "1.0.0",
  "description": "budget-tracker",
  "main": "index.js",
  "scripts": {
    "build": "turbo run build",
    "start:prod": "turbo run build && node apps/api/dist/main",
    "start:dev": "turbo run start:dev",
    "start": "node apps/api/dist/main",
    "postinstall": "prisma generate --schema=./api/prisma/schema.prisma"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "turbo": "^1.10.12"
  },
  "workspaces": [
    "apps/*"
  ]
}
