{
  "name": "budget-tracker",
  "version": "1.0.0",
  "description": "budget-tracker",
  "main": "index.js",
  "scripts": {
    "build": "turbo run build",
    "start:prod": "turbo run build && node apps/api/dist/main",
    "start:dev": "turbo run start:dev",
    "start": "node apps/api/dist/main",
    "generate": "prisma generate --schema=./apps/api/prisma/schema.prisma",
    "env:up": "docker-compose build && docker-comppose up -d",
    "env:down": "docker-compose down"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "turbo": "^1.10.12"
  },
  "workspaces": [
    "apps/*"
  ]
}
